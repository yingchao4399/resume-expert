# 更新日志

## 1.7.3

- 岗位、JD 或原简历变化后保留但锁定旧分析，并用材料修订号拒绝迟到的 AI 结果。
- ATS 关键词只检查最终简历正文；Mock 不再为非示例材料生成固定人物、公司或业绩数据。
- 自动证据匹配降级为候选关联，用户确认后才视为可信；人工改写、证据修改或删除会触发复核和简历过期。
- AI 只接收当前 JD 相关的已确认证据，补证和 Flowise 候选按来源去重并保留事实级来源。
- 备份升级至版本 3，修复合并副本的证据外键；损坏数据按文档、证据、投递和复盘分别恢复。
- 工作流按四阶段依赖计算，Prompt 与 Schema 使用一致的自适应结果数量。

## 1.7.2

- 开发者工作台的只读地图升级为受约束的 React Flow 编辑器，支持位置、说明、可选节点和 AI 执行参数调整。
- 增加循环、类型、起止节点、必经门禁、不可达节点校验；材料、证据、最终简历与导出门禁不可删除或绕过。
- 工作流采用“草稿 → 测试 → 发布 → 回滚”，保留最近 10 个版本并支持差异比较。
- Prompt、模型、Provider 或超时变更需通过 7 天内真实评测；未达标只发布实验版本，Flowise 主流程适配器未批准前不会接管业务流程。
- Studio 工作流单独保存在 IndexedDB，不升级简历业务 Schema；发布的 DirectLLM/Mock 参数由 TypeScript 执行器安全应用。

## 1.7.1

- 接入仅绑定 `127.0.0.1:3200` 的 Flowise 实验服务，浏览器不接收 API Key。
- 增加“新手项目梳理”、DirectLLM/Flowise 对比、Mock 回退和统一运行追踪。
- Flowise 输出必须通过 `ProjectEvidenceDraft` Schema，用户确认后才写入证据库候选。
- 本机 Flowise 升至 3.1.4；上游依赖审计仍有高危项，因此默认禁用并明确警告。

## 1.7.0

- 新增可通过高级设置开启的 `/studio` 开发者工作台。
- 增加只读工作流地图、本机 IndexedDB 运行追踪、测评中心和结构化开发记录。
- AI API 响应携带脱敏运行元数据；Trace 自动清理且不进入业务文档 Schema 或 Git。

## 1.6.2

- 新增 24 个纯合成冻结案例和确定性 AI 质量评分器。
- 增加 Mock、真实模型、报告与基线比较命令，真实模型评测仅允许本机显式运行。
- CI 新增评测案例校验与 Mock 回归，不提交真实简历、API Key 或模型原始输出。

## 1.6.1

- 本地开发与 CI 统一升级到 Node.js 24 LTS，并更新 GitHub Actions 的 Node 24 运行时版本。
- 增加生产依赖高危漏洞检查，并将 Next.js 升级到修复高危依赖的 16.3.0。
- 拆分简历 Store 与面试复盘页面的内部结构，不改变业务数据 Schema 和用户操作行为。
- 补充持久化与面试复盘回归测试。

## 1.6.0

- 引入最终简历草稿、已确认和已过期状态，只有已确认简历可进入交付。
- 统一侧栏与交付页的本地 ATS 就绪度评分，并区分 AI 简历诊断分。
- 增加 AI 连接测试、请求超时和认证、模型、地址、限流、网络、超时错误分类。
- 增强中文 PDF 提取、录音播放与删除、损坏数据恢复、未保存修改保护和保存状态反馈。
- 修复 A4 打印双重页边距和多页简历异常留白，并增加完整 Chromium 端到端测试与 CI。

## 1.5.0

- 新增本地投递记录：公司、岗位、JD、固定状态、日期、备注和关联简历。
- 新增各状态数量、面试率和 Offer 率统计，并明确不作因果承诺。
- 面试复盘自动持久化转写、分析结果和录音元数据，可关联投递与实际简历。
- 删除被引用简历时明确警告；投递和复盘记录保留并解除关联。
- JSON 备份升级到版本 2，持久化升级到 Schema 5。

## 1.4.0

- 将工作流归并为材料、分析、制作、交付四个主阶段。
- 保留全部内部细粒度步骤和已有数据，旧文档当前步骤可原样恢复。
- 每个阶段显示完成状态、阻塞原因和可点击的下一步入口。
- 面试准备归入分析阶段，面试复盘继续保持独立模块。

## 1.3.0

- 新增全局个人经历证据库，支持导入候选、人工核验、编辑和删除。
- 简历 bullet 升级为稳定 ID、来源类型和证据关联记录。
- 展示原始内容、AI 改写、人工修改及关联证据。
- AI 仅接收已确认的证据作为额外事实来源，证据不足时继续通过经历补证完善。
- JSON 备份与持久化升级到 Schema 4，并连续迁移旧数据。

## 1.2.0

- 新增 ATS 经典、现代简洁、紧凑专业三款单栏模板。
- 支持字体、字号、行距、区块间距、页边距、强调色和项目符号调整。
- 支持区块拖拽排序与显隐，每个岗位版本独立保存。
- 网页预览、A4 打印/PDF 和 DOCX 共用同一排版配置。
- 文档持久化升级到 Schema 3。

## 1.1.0

- 支持在浏览器本地提取 PDF 与 DOCX 简历文本。
- 支持提取结果预览、编辑和可选 AI 结构化。
- 增加当前岗位版本与完整文档库的 JSON 备份、合并和替换恢复。
- 文档持久化升级到 Schema 2，并兼容迁移 1.0.0 的本地数据。
- 原始文件、API Key、录音与运行状态不会写入备份。

## 1.0.0

- 完成个人简历优化工作台 MVP。
