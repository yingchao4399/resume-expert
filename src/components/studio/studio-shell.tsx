"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import { ArrowLeft, BarChart3, Code2, ExternalLink, FlaskConical, Route, Settings2 } from "lucide-react";
import { WorkflowMap } from "@/components/studio/workflow-map";
import { TracePanel } from "@/components/studio/trace-panel";
import { Button } from "@/components/ui/button";
import { RELEASE_RECORDS } from "@/config/releases";
import { isStudioEnabled } from "@/lib/studio/settings";

const baseline = { schema: "100%", facts: "100%", recall: "100%", supplement: "100%", cases: 24 };

export function StudioShell() {
  const [enabled, setEnabled] = useState<boolean | null>(null);
  const [tab, setTab] = useState("workflow");
  useEffect(() => setEnabled(isStudioEnabled()), []);
  if (enabled === null) return <div className="p-8 text-sm text-neutral-500">正在读取本机设置…</div>;
  if (!enabled) return <div className="mx-auto flex min-h-screen max-w-xl items-center p-8"><div className="w-full rounded-xl border bg-white p-6"><Settings2 className="h-6 w-6"/><h1 className="mt-4 text-lg font-semibold">开发者工作台尚未开启</h1><p className="mt-2 text-sm leading-6 text-neutral-600">返回简历助手，在“AI 设置 → 高级功能”中开启。该开关不是安全认证，开发台仅适合本机使用。</p><Button asChild className="mt-5"><Link href="/">返回简历助手</Link></Button></div></div>;
  const tabs = [{ id: "workflow", label: "工作流地图", icon: Route }, { id: "traces", label: "运行追踪", icon: FlaskConical }, { id: "evals", label: "测评中心", icon: BarChart3 }, { id: "releases", label: "开发记录", icon: Code2 }, { id: "flowise", label: "Flowise 实验室", icon: ExternalLink }];
  return <div className="min-h-screen bg-neutral-50"><header className="flex min-h-14 flex-wrap items-center justify-between gap-3 border-b bg-white px-5 py-2"><div className="flex items-center gap-3"><Button asChild variant="ghost" size="sm"><Link href="/"><ArrowLeft />简历助手</Link></Button><div className="h-5 w-px bg-neutral-200"/><div><h1 className="text-sm font-semibold">开发者工作台</h1><p className="text-[10px] text-neutral-400">可审计工作流，不展示隐藏思维链</p></div></div><span className="rounded border px-2 py-1 text-[10px] text-neutral-500">本机高级功能</span></header><div className="mx-auto grid max-w-7xl gap-6 p-6 lg:grid-cols-[210px_1fr]"><nav className="space-y-1">{tabs.map((item) => <button key={item.id} onClick={() => setTab(item.id)} className={`flex w-full items-center gap-2 rounded-md px-3 py-2 text-left text-xs ${tab === item.id ? "bg-neutral-900 text-white" : "text-neutral-600 hover:bg-neutral-100"}`}><item.icon className="h-4 w-4"/>{item.label}</button>)}</nav><main className="min-w-0 space-y-5">{tab === "workflow" && <><div><h2 className="text-lg font-semibold">产品工作流</h2><p className="mt-1 text-sm text-neutral-500">由 TypeScript 业务规则生成的只读视图，状态与当前岗位文档一致。</p></div><WorkflowMap /></>}{tab === "traces" && <TracePanel />}{tab === "evals" && <><div><h2 className="text-lg font-semibold">测评中心</h2><p className="mt-1 text-sm text-neutral-500">V1.6.2 冻结 Mock 基线；真实模型评测仅在本机手动运行。</p></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{Object.entries(baseline).map(([key,value]) => <div key={key} className="rounded-lg border bg-white p-4"><p className="text-[11px] uppercase text-neutral-400">{key}</p><p className="mt-2 text-2xl font-semibold">{value}</p></div>)}</div><div className="rounded-lg border bg-white p-4 font-mono text-xs">npm run eval:validate<br/>npm run eval:mock<br/>npm run eval:ai<br/>npm run eval:compare<br/>npm run eval:report</div></>}{tab === "releases" && <><div><h2 className="text-lg font-semibold">开发记录</h2><p className="mt-1 text-sm text-neutral-500">展示计划、可验证变更、测试和发布，不记录私有思维链。</p></div><div className="space-y-3">{RELEASE_RECORDS.map((release) => <article key={release.version} className="rounded-lg border bg-white p-4"><div className="flex justify-between gap-3"><div><h3 className="text-sm font-semibold">V{release.version} · {release.goal}</h3><p className="mt-2 text-xs text-neutral-500">{release.modules.join(" · ")}</p></div><span className="text-[10px] uppercase text-neutral-400">{release.status}</span></div><p className="mt-3 border-t pt-3 text-xs text-neutral-600">验证：{release.tests}{"commit" in release ? ` · ${release.commit}` : ""}</p></article>)}</div></>}{tab === "flowise" && <div className="rounded-xl border bg-white p-6"><ExternalLink className="h-6 w-6"/><h2 className="mt-4 text-lg font-semibold">Flowise 实验室</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-neutral-600">V1.7.0 先建立入口。V1.7.1 将在 127.0.0.1:3200 接入本机 Flowise，主应用继续负责 Schema、证据确认和业务状态。</p><Button asChild variant="outline" className="mt-5"><a href="http://127.0.0.1:3200" target="_blank" rel="noreferrer">尝试打开 Flowise</a></Button></div>}</main></div></div>;
}
